#!/usr/bin/env bash

set -euo pipefail

app="$1"
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
script="$SCRIPT_DIR/modules/webapp-$app.sh"

[ -f "$script" ] || {
  echo "unknown webapp: $app" >&2
  exit 1
}

exec "$script"
