#!/usr/bin/env bash

set -euo pipefail

workflow="$1"
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
script="$SCRIPT_DIR/modules/workflow-$workflow.sh"

[ -f "$script" ] || {
  echo "unknown workflow: $workflow" >&2
  exit 1
}

exec bash "$script"
