# %s - String format

How do I format a string in Go?

---

Use `%s`.

```go
fmt.Printf("I am %s years old\n", "way too many")
// I am way too many years old
```

## When to use
- Formatting string values explicitly
- More specific than `%v`